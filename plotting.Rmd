---
title: "Untitled"
output: html_document
---
```{r}
library(ggplot2)
library(dplyr)
library(ncdf4)
library(lubridate)
```

```{r}
files <- Sys.glob("processed/*")
df <- read.csv(files[1])
df_grp_doy <- group_by(df, doy) 
df_doy <- summarize(df_grp_doy, mean(cica)) 

plot(df$vpd)

#df_grp_hod <- group_by(df, hour) 
#df_hour <- summarise_all(df_grp_hod, funs(mean)) 
#plot(df_hour$vpd)

#for (fname in files[2:length(files)]) {
#  df <- read.csv(fname)
#  site_name <- substr(fname, 11, 16)
#  print(ggplot(df,aes(doy, cica, col=site_name ))+geom_line())
#  
#}

```



```

